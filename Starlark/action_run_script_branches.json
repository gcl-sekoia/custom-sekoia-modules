{
  "name": "Run Starlark Script (branches)",
  "description": "Execute a Starlark (Python-like) script that transforms data and routes the flow to one of three branches. `return <data>` fires the centered branch; `return output(\"left\"|\"right\", <data>)` fires a side; `return stop()` halts.",
  "uuid": "769a898b-b4f2-48c3-9d1a-69ca3fcec85e",
  "docker_parameters": "RunScriptBranchesAction",
  "arguments": {
    "title": "RunScriptArguments",
    "type": "object",
    "properties": {
      "script": {
        "title": "Script",
        "description": "Starlark script defining `def main(arguments): ...`. Return data (centered branch), output(name, data) for a side/custom branch, or stop() to halt.",
        "type": "string"
      },
      "arguments": {
        "title": "Arguments",
        "description": "Input object passed to the script as `arguments` (typically wired from previous playbook nodes).",
        "type": "object"
      }
    },
    "required": [
      "script"
    ]
  },
  "outputs": {
    "left": "Fires when the script returns left(<data>).",
    "main": "Fires when the script returns data directly (the centered, primary branch).",
    "right": "Fires when the script returns right(<data>)."
  },
  "results": {},
  "slug": "run_script_branches"
}
