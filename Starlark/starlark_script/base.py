from sekoia_automation.module import Module


class StarlarkModule(Module):
    """The scripting module carries no configuration of its own -- each action
    is fully described by its `script` argument."""
